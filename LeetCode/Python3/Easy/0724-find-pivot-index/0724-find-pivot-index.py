class Solution:
    def pivotIndex(self, nums: List[int]) -> int:
        total = sum(nums)
        cnt = 0
        

        for i in range(len(nums)):
            
            if left_ind == 0:
                left, right = 0, total
            elif right_ind == 0:
                left, right = total, 0
            else:
            
            left += n
            right -= n
            if left == right

        